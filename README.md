my website


## Admin Dashboard
Open `admin.html` after signing in with an account whose `profiles.role` is `admin`.
Run `supabase/admin-upgrade.sql` and then `supabase/make-admin.sql` in Supabase SQL Editor.
The dashboard lets the owner review customer quote requests and edit package/appliance prices. Public product cards read active products from `public.products`.
