# Project Media Manager

Run `supabase/projects-media.sql` once in Supabase SQL Editor.

Then open Admin Dashboard -> Projects & Media. Admins can upload images/videos, edit project information, hide/show projects, and delete them. Public `projects.html` reads active projects from Supabase.

Uploaded media is stored in the Supabase Storage bucket `project-media`.
