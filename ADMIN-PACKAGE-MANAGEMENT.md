# Aphotech Solar Admin Package Management v6

The admin product manager supports Edit, Hide/Show, Delete, and Add. Existing RLS from `supabase/admin-upgrade.sql` grants admins insert/update/delete access to `public.products`; no additional SQL is required if that setup has already run successfully.
